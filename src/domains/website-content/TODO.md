# Website Content TODO

- Re-implement the old n8n Google Maps scrape category behavior:
  - create missing `lead_categories` rows from scrape category labels
  - link each scraped lead to the created/found category via `leads.category_id`
  - keep category normalization aligned with existing `normalized_name` conventions
- Target: continue implementation tomorrow.
